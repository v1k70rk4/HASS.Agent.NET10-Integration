{
  "config": {
    "flow_title": "{name}",
    "step": {
      "user": {
        "title": "Add HASS.Agent device",
        "description": "MQTT devices are discovered automatically. Choose a manual connection method below.",
        "menu_options": {
          "ha_api_info": "HA API (WebSocket)",
          "local_api": "Local HTTP API (notifications only)"
        }
      },
      "confirm": {
        "title": "Confirm",
        "description": "Do you want to start set up?"
      },
      "local_api": {
        "title": "HASS.Agent .NET10 Local HTTP API",
        "description": "This adds the device to HA using the local HTTP API. Only notifications are supported. Use MQTT or HA API for full functionality.",
        "data": {
          "host": "Host",
          "port": "Port",
          "ssl": "Use SSL",
          "api_key": "API key (from agent General settings)"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect"
    },
    "abort": {
      "single_instance_allowed": "Already configured. Only a single configuration possible.",
      "no_devices_found": "No devices found on the network.",
      "already_configured": "Device is already configured.",
      "ha_api_auto_discovery": "HA API devices are discovered automatically. Enable HA API in the HASS.Agent .NET10 Windows client settings and make sure the agent is running — the device will appear in Home Assistant within seconds."
    }
  },
  "options": {
    "step": {
      "init": {
        "data": {
          "default_notification_title": "Default notification title. Space to disable."
        },
        "title": "Configure HASS.Agent .NET10"
      }
    }
  },
  "device_automation": {
    "trigger_type": {
      "notifications_mqtt": "When a notification with a specific action has been pressed (MQTT)",
      "notifications_event": "When a notification with a specific action has been pressed (Event)"
    }
  },
  "entity": {
    "event": {
      "notification_action": {
        "name": "Notification actions"
      }
    }
  },
  "issues": {
    "restart_required": {
      "title": "Device renamed - restart required",
      "fix_flow": {
        "step": {
          "confirm_restart": {
            "title": "Restart required",
            "description": "Device name has been changed, restart of Home Assistant is required to properly reload notify service for {name}, click submit to restart now."
          }
        }
      }
    }
  }
}
